# ==============================================================================
# Arkidi Frontend — Production Environment Template
# ==============================================================================
# Vite automatically loads .env.production during `vite build`.
# Copy this file to .env.production and update the API URL to match your
# production backend endpoint.

VITE_API_BASE_URL=https://api.arkidi.org/api/v1
VITE_APP_NAME="Arkidi Platform"
VITE_DEFAULT_LANGUAGE=en
